# Phase 3: Implement Simple Flow Diagrams (No Subgraphs)

- [x] Extend `src/parser/grammar.pegjs` to support `type flow` diagrams. Must parse: `direction LR/TB`, `title`, node declarations (`service`, `component`, `external`, `actor`, `datastore`, `label`) with properties (`fill`, `border`, `sublabel`, `shape`), edges (`-->`, `<--`, `<-->`) with labels, `annotation` elements with `near` and `side` properties. Keep the existing sequence grammar intact — the grammar should handle both types.
- [x] Update `src/parser/parser.ts` if needed to handle the extended grammar and produce FlowDiagram AST nodes. (No changes needed — parser.ts works as-is. Updated ast.ts with direction, FlowNodeKind, FlowAnnotation types.)
- [x] Create `src/layout/flow-layout.ts` — Sugiyama-style layered layout with cycle removal (DFS back-edge detection), longest-path layer assignment, barycenter crossing minimization, and coordinate assignment. Supports LR and TB directions. Handles annotation positioning near nodes with side offsets.
- [x] Create `src/render/flow-renderer.ts` — renders flow diagrams to SVG. Nodes use correct shapes (service=doubleBorder+hero, component=rect+dotgrid/crosshatch, actor=stick figure, datastore=cylinder). Edges are straight lines with spread connection points for parallel edges. Labels use multiline wrapping. Annotations rendered as italic text.
- [x] Update `src/render/svg.ts` to dispatch flow diagrams to the new layout and renderer.
- [x] Write example file `examples/responses-api.n9` matching the "Responses API orchestrates agent loop" reference image. Actor USER, service RESPONSES API (hero), component MODEL (dotgrid), component RUNTIME CONTAINER (crosshatch), 5 edges with labels, annotation "Feedback loop (until complete)".
- [x] Run `npx tsx src/render-examples.ts` to render all examples. Output matches reference: USER left, RESPONSES API center, MODEL top-right, RUNTIME CONTAINER bottom-right. Edge labels separated via spread connection points and multiline wrapping. Tuned LAYER_GAP=240, NODE_GAP=120, spread=80.
- [x] Ensure the existing sequence diagrams still render correctly after the grammar changes. Both codex-debugging.n9 and streaming-shell.n9 render identically to before.
