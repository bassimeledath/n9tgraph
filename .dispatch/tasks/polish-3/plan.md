# Polish Pass 3: Final Edge Routing and Proportions

Targeted fixes for the remaining visual gaps between output and reference images.

- [x] Compare `tools-overview.png` vs reference side by side. In the reference, MODEL is closer to the subgraph and edges enter HORIZONTALLY into each tool node (not fanning out diagonally). The subgraph is more compact with tighter internal spacing. The code blocks (COMMAND, JSON, ACTION) are horizontally aligned with their connected tool. Fix: either adjust the layout to position MODEL closer and use horizontal edges, or modify edge routing to avoid the wide fan-out. — Fixed by reducing LAYER_GAP (160→120) and NODE_GAP (80→55 base), making the subgraph more compact and MODEL closer.
- [x] Compare `responses-api.png` vs reference. The reference has more VERTICAL space between the MODEL path (upper right) and the RUNTIME CONTAINER path (lower right). The RESPONSES API node is vertically centered. Currently our diagram is too flat/horizontal. Increase NODE_GAP or manually position MODEL higher and RUNTIME CONTAINER lower relative to RESPONSES API. — Fixed with dynamic per-layer NODE_GAP (135px for layers with labeled multi-edge pairs) and taller service nodes (200px for services with 2+ multi-pair groups).
- [x] Compare `layered-architecture.png` vs reference. The reference shows horizontal arrows within each row (Providers→App Wiring+UI, Service→Runtime→UI, Types→Config→Repo). Currently we don't have horizontal within-row edges — the edges from Providers to Service are vertical but Providers→App Wiring should be horizontal. Check the .n9 file has the right edges and verify the layout places connected same-layer nodes at the same y level. — Fixed by adding horizontal same-row edges to .n9 file and implementing iterative same-layer edge detection in the TB layout algorithm. Now renders with correct 4-layer structure matching reference.
- [x] Compare `session-multiplexer.png` vs reference one more time. The reference has clean horizontal parallel edge pairs. Our output is close but verify the edges are truly horizontal and labels are positioned clearly. — Verified: edges are clean horizontal pairs with well-positioned labels. Structure matches reference.
- [x] Re-render all 11 examples and verify no regressions — All 11 render successfully with no regressions.
- [x] Copy final PNGs to ~/Downloads/n9tgraph-output/ — Done, 11 PNGs copied.
- [x] Write summary to .dispatch/tasks/polish-3/output.md
