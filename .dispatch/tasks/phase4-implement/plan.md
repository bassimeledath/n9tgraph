# Phase 4: Flow with Subgraphs + Advanced Features

- [x] Extend `src/parser/grammar.pegjs` to support: `subgraph` blocks with child nodes, `overflow` indicators, `codeblock` elements with `title` and `code` properties, node `sublabel` property, `border: double` property, numbered step indicators. The grammar must handle nested blocks (subgraph containing nodes). Keep existing flow and sequence support intact. — Added makeId helper, FlowSubgraphStmt, FlowOverflowStmt, FlowCodeblockStmt, EdgeText rule, circle node kind, PropertyValue numbers, edge properties block.
- [x] Update `src/parser/ast.ts` with types for Subgraph (with children), CodeBlock, Overflow, and any missing properties. — Added SubgraphChildOrder, CodeBlock, updated Subgraph with properties/childOrder, FlowEdge with properties, FlowNodeKind with circle, Properties with border/sublabel/code/step.
- [x] Extend `src/layout/flow-layout.ts` to handle subgraphs/clusters: (a) identify which nodes belong to which subgraph, (b) lay out subgraph children internally (vertical or horizontal stacking based on subgraph direction), (c) compute subgraph bounding box (children bbox + padding + title), (d) treat subgraph as a composite node in the top-level layout, (e) handle cross-boundary edges (edges from nodes outside to nodes inside a subgraph). Also handle code block positioning based on edge connections. — Flat layout approach: all nodes (top-level + subgraph children + codeblocks) positioned by Sugiyama algorithm, then subgraph bounding boxes computed post-hoc. Added PositionedSubgraph, PositionedOverflow, PositionedCodeBlock types.
- [x] Extend `src/render/flow-renderer.ts` to render: (a) subgraph backgrounds with pattern fill and green border at reduced opacity, (b) subgraph titles (mixed case, top-left inside), (c) child nodes within subgraph bounds, (d) double-border nodes, (e) code blocks (title + monospace body, no border), (f) overflow ellipsis indicators (3 small green circles), (g) sub-labels below main labels, (h) numbered step circles on edges, (i) cross-boundary edges that pass cleanly through subgraph borders. — All rendering features implemented. Circle nodes, sublabels, border:double for any kind, edge step circles via numberedCircle.
- [x] Write example `examples/tools-overview.n9` matching the "Language models use tools" reference. MODEL (dotgrid) → Tools subgraph (crosshatch) containing SHELL (double-border), overflow "...", FUNCTION (double-border), COMPUTER USE (double-border). Each tool node connects to a code block (COMMAND, JSON, ACTION).
- [x] Write example `examples/session-multiplexer.n9` matching the "Responses API multiplexes sessions" reference. RESPONSES API (hero, sublabel "SESSION MULTIPLEXER") ↔ Runtime container subgraph (crosshatch) containing SESSION ID: 101 and SESSION ID: 102 with parallel bidirectional edges.
- [x] Write example `examples/skill-loading.n9` matching the "Responses API uses skills: Registry → Bundle → Runtime" reference. RESPONSES API (hero with sub-text) → SKILL REGISTRY → SKILL BUNDLE STORAGE → CONTAINER RUNTIME, with numbered step circles (1, 2, 3) and edges showing the flow.
- [x] Write example `examples/container-egress.n9` matching the "Container egress proxy controls network access" reference. USERS → RESPONSES API (hero) → Compose session subgraph containing MAIN CONTAINER and SIDECAR PROXY, plus NETWORK circle node. Numbered steps (1, 2, 3) with annotation text blocks.
- [x] Run `npx tsx src/render-examples.ts` to render all examples. Verify the new flow examples match references. Adjusted skill-loading layout (targets as direct edges for vertical stacking).
- [x] Ensure all previous examples (sequence diagrams, simple flow) still render correctly. — All 3 existing examples (responses-api, streaming-shell, codex-debugging) render identically.
