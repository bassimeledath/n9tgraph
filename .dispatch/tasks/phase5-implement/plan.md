# Phase 5: Implement Card Layouts

- [x] Extend `src/parser/grammar.pegjs` to support `type card` diagrams. Must parse: `node` (standalone labeled box with properties), `container` blocks with child elements, `card` elements (icon + title + body), `overflow` inside containers, `edge_in` (incoming arrow with icon/label), `hanging` (label dangling off a node), edges between nodes and containers. Keep existing flow and sequence grammar intact. — Added CardBody, CardStatement, CardNodeStmt, CardContainerStmt, CardCardStmt, CardOverflowStmt, CardEdgeInStmt, CardHangingStmt, CardEdgeStmt, CardSide rules. Also added QuotedString support to TitleLine for multiline titles.
- [x] Update `src/parser/ast.ts` with types for CardDiagram, CardNode, CardContainer, CardCard, EdgeIn, EdgeOut, HangingLabel. — Replaced placeholder Card/Container types with CardNode, CardCard, CardContainer, CardEdgeIn, CardHangingLabel, CardEdge. Updated CardDiagram with nodes, containers, edges, edgesIn, hangingLabels fields.
- [x] Create `src/layout/card-layout.ts` — spatial layout for card diagrams. Top-level nodes centered/anchored. Containers sized to fit children (cards in horizontal flex row). Cards auto-sized from content. Simple straight connections between elements. Hanging labels offset from parent node. — Implements vertical stacking layout with centered nodes, horizontal card flex inside containers, word-wrapped body text, edge_in positioning from any side, hanging label connector positioning.
- [x] Create `src/render/card-renderer.ts` — renders card diagrams to SVG. Cards with dark bg (#111111), gray border (#333333), white title (bold), white body (70% opacity), icon left of title. Containers with pattern fill. Pill-shaped nodes. Edge-in arrows with word-wrapped labels. Hanging labels with icons and connector lines. Overflow dots in white.
- [x] Update `src/render/svg.ts` to dispatch card diagrams. — Added imports for layoutCard and renderCardDiagram, replaced error throw with renderCardDiagramSvg function.
- [x] Write example `examples/codex-knowledge.n9` matching the "The limits of agent knowledge" reference image. CODEX'S KNOWLEDGE pill node at top, container below with 3 cards (Google doc, Slack message, Tacit knowledge), edge_in with "Encode into codebase as markdown", hanging label "Unseen knowledge to Codex", overflow "..." in container.
- [x] Run `npx tsx src/render-examples.ts` and verify the card layout matches the reference. — All 8 diagrams render successfully. Card output matches reference: multiline title, pill node with dotgrid, edge_in arrow with label, hanging label with chain-broken icon, container with 3 cards + overflow dots.
- [x] Ensure all previous diagrams still render correctly. — Verified: responses-api, streaming-shell, codex-debugging, container-egress, session-multiplexer, skill-loading, tools-overview all render identically to before.
