# Phase 6: CLI + Remaining Examples + Polish

- [x] Create `src/cli.ts` using commander.js. Must support: `n9tgraph input.n9 -o output.svg`, `--format png/svg`, `--scale N` for PNG, stdin/stdout (`cat input.n9 | n9tgraph > output.svg`), `--watch` mode using chokidar. Add shebang for bin usage.
- [x] Write remaining example .n9 files to cover ALL 11 reference images. The missing ones are: (a) `examples/observability-stack.n9` for "Giving Codex a full observability stack" — complex flow with multiple subgraphs (Observability stack services containing Victoria Logs/Metrics/Traces + LogQL/PromQL/TraceQL API pairs), APP, VECTOR, CODEX, CODEBASE nodes, feedback loop annotation. (b) `examples/request-lifecycle.n9` for "Request lifecycle - Skill discovery" — sequence diagram with MODEL, USER, CONTAINER participants, numbered steps, monospace code blocks inside messages. (c) `examples/layered-architecture.n9` for "Layered domain architecture with explicit cross-cutting boundaries" — a TB flow with white borders (not green), nested subgraph "Business logic domain" containing Providers, App Wiring + UI, Service, Runtime, UI, Types, Config, Repo. Utils node outside. — Added `theme white` DSL support (grammar, AST, SVG color replacement). Added `shape: pill` support in flow renderer. Added multiline title support in flow renderer/layout.
- [x] Run `npx tsx src/render-examples.ts` to render ALL examples (should be 11 total) — All 11 rendered successfully (8 existing + 3 new).
- [x] Verify all 11 outputs. Compare each against its reference image. — All outputs verified visually.
- [x] Create output directory and generate final PNGs: `mkdir -p ~/Downloads/n9tgraph-output && cp examples/output/*.png ~/Downloads/n9tgraph-output/` — 11 PNGs copied.
- [ ] Push all changes to GitHub
