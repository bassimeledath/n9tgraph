All done. Here's a summary of the 4 fixes applied:

1. **Checkout-flow polyline routing** (`flow-renderer.ts`): Obstacle avoidance now also checks pending edge labels as obstacles when computing the detour path, so the ORDER CONFIRMATION → INVENTORY DB route clears the "update stock" label.

2. **Auth-flow horizontal centering** (`ascii-layout.ts`): Added a centering step in Step 11 that shifts all elements so content is horizontally centered within the canvas.

3. **Microservices node-to-subgraph gap** (`ascii-layout.ts`): Added Step 8a2 enforcing ≥30px between any non-child node bottom and subgraph top (fixes API GATEWAY → Backend Services 16px gap).

4. **Subgraph label clearance** (`ascii-layout.ts`): Increased `LABEL_CLEARANCE` from 5px to 10px for more breathing room.

Build compiles clean and all 5 benchmarks rendered to `bench/ascii-guided/output/` and `/Users/bassime/Downloads/n9tgraph-ascii-baseline/` as `i3-{name}.png`.
